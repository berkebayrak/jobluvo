Input: single line text field, 36px, strong border that turns black on focus. Error is the only red in the system.

```jsx
<Input label="Lane name" value="Strategy director, US remote" />
<Input label="Email" error="Enter a work email" />
```

Read only fields sit on surface-1 with subtle text. No inner shadows, no floating labels.
