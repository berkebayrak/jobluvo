Toggle: 28x16 switch. On is a black track with a white knob, off is a white track with a grey knob. Also used to run or pause a lane.

```jsx
<Toggle on label="Pause on unknown questions" description="The application waits for you instead of guessing." />
```

Disabled is 50% opacity on the row. Never animate anything except the knob position.
