Toast: black pill at the bottom centre, white 13px text, optional grey Undo. The only inverted surface besides primary buttons.

```jsx
<Toast text="Applying to Datadog. Resume tailored, submitting now." actionLabel="Undo" onAction={undo} />
```

Dismiss after 3 seconds. Never stack more than one.
